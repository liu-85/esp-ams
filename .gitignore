# ============================================================
# 构建产物
# ============================================================
/dist/
/build/
/tools/build/
/esp32c3-ams-mpy.zip
/esp32s3-ams-mpy.zip
/esp32-ams-mpy.zip
*.exe
*.o
*.a

# 编译期生成的板型选择文件（由 tools/build_mpy.py / make_firmware_bin.py 生成，
# 不要提交：它决定设备加载哪一份引脚表，属于构建产物而非源码）
python_code/board_select.py
board_select.py

# 现场接线覆盖文件（"我这块板接线和别人不一样"时放在板子上用的，不进版本库）
board_override.py
python_code/board_override.py

# ============================================================
# 设备上生成的敏感配置（含 WiFi 密码、打印机访问码，绝对不能提交）
# 首次配置后会由设备自己生成，忘掉里面的密码只能重新配一次
# ============================================================
config.json
wifi.dat

# ============================================================
# 设备上生成的诊断数据（启动计数 / 上次复位原因）
# 排查复位循环时会重新生成，不需要进版本库
# ============================================================
boot_stat.json

# ============================================================
# 排查过程中的临时探测输出（串口 dump、端口枚举、REPL 回显……）
# 这些是随手落地的中间文件，不是资料。有价值的部分请整理成 tools/ 下的
# 脚本或写进记忆，别让它们进版本库（其中常有几十 KB 的原始串口回显）
# ============================================================
.workbuddy/_*

# ============================================================
# 交叉编译产物
# ============================================================
*.mpy

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ============================================================
# IDE / 编辑器
# ============================================================
.idea/
.vscode/
*.iml

# ============================================================
# 操作系统
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
