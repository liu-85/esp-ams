# 2026-09-17

## 刷机后无 AP：根因确认与修复（延续 09-16 晚 22:30 的串口排查）

- 用户刷的是 CI 固件（release latest = 09e749b「说明」，用户自己 push 的），
  板子起不来热点，串口报 `RuntimeError: Wifi Unknown Error 0x0101`，
  崩在 network_model.py:76 建 WLAN 那一步。
- **根因：0x0101 = ESP_ERR_NO_MEM**（MicroPython 的 WiFi 错误表只认
  0x3001~0x300E，落在外就显示 Unknown Error，非常误导）。
  esp_wifi_init() 要一次性申请约 24KB **连续**堆内存；main.py 先
  `from AMS_WEB import main_task` 把应用加载进堆（吃 ~107KB、切碎堆），
  剩 92KB 空闲也拼不出连续 24KB → 失败。
- **修法**：main.py 顶部、import 应用之前先 `network.WLAN(AP_IF)` +
  `network.WLAN(STA_IF)`。空堆一次成功；之后再调只花 16 字节（对象复用）。
  串口实测：空堆 ~200KB → 成功；WiFi 总代价 AP 24,576B + STA 16B。
- 桌面自测 116/116 过；提交 **6288725**（纯增量，原逻辑原样保留）。
- **板子早上没插**（枚举不到任何串口/CH343）。推板脚本已备好：
  `%TEMP%\push_main.ps1` —— Ctrl-C 抢 REPL → 粘贴模式 base64 写入
  main.py → SHA256 回读校验 → RTS 复位 → 抓 40 秒启动日志 → 自动判定
  （失败关键字 0x0101/Traceback；成功关键字 配置热点已打开/AMS_WIFI/192.168.4.1）。
  板子插上后一条命令完成验证。
- 待办：①板子插上跑 push_main.ps1，确认 AMS_WIFI 出现；②提醒用户 push
  6288725 触发 CI 重编固件（否则下次整烧的还是坏 main.py）。
- 顺手清了根目录临时探针 `_bin_probe.py` / `_boot_sim.py`。
- 串口工具链（都在 %TEMP%，复用价值高）：`mpy_paste.ps1`（粘贴模式执行
  ASCII 代码）、`ams_ctrlc2.ps1`（RTS 复位 + 持续 Ctrl-C 抢干净 REPL）、
  `ams_serial.ps1`（被动监听 + 复位抓日志）。pyserial 没装，全靠 .NET
  SerialPort。

## 下午：重启循环修完后，用户反馈「连接 AP 失败」——定位为硬件侧（能收不能发）

启动链已经完全干净（0 复位 / 0 traceback / 0 OSError，热点 0.57s 起来、
Web 2.76s 起来，桌面自测 116/116，提交 51ec834），但手机/电脑**搜不到
AMS_WIFI**。逐层排除，最后确认是**板子射频发射侧的问题，软件无解**：

| 测什么 | 结果 |
|---|---|
| 板子自报 AP 状态 | `active=True`、`essid=AMS_WIFI`、`authmode=3`、`channel=1`、`hidden=False`、`max_clients=10`、`ifconfig=192.168.4.1` —— **全对** |
| 板子自己的 STA `scan()`（AP 关掉时） | **扫到 14 个网**，xiaomi2 才 -40dBm → **RX 正常、天线正常** |
| 板子的 STA 连路由器 | 连上 xiaomi2，DHCP 拿到 192.168.2.153 → **TX 也正常** |
| Windows `netsh wlan show networks` | 反复扫，AMS_WIFI **0 命中**（同批能扫到 2.4G 的 xiaomi2 / FAST_2.4G） |
| Windows 主动 `netsh wlan connect name=AMS_WIFI` | 请求"成功"但仍停在原网络，拿不到 192.168.4.x → 定向 probe 也没回应 |
| 擦 NVS(0x9000+0x6000) + phy_init(0xf000+0x1000) | 无效 |
| **整片 erase_flash + 只烧官方纯净固件**（无任何应用代码） | 裸 AP 依然 0 命中 → **彻底排除软件/配置/应用** |
| 开放网络 / WPA2 / 先 config 后 active / 信道 1·6·11 / 降功率到 2dBm | 全部无效（降功率那次 1/3 是误报，A/B/A 各 8 次复测均为 0） |
| eFuse summary | 正常，无 TX 功率限制迹象 |

- 一个耐人寻味的旁证：`ap.config(channel=6)` 之后 `ap.config("channel")`
  **永远读回 1**（MicroPython 读的是 `esp_wifi_get_channel`，即射频实际
  信道）—— 说明 AP 的射频层其实没有真正跑起来，但 `active(True)` 没报错。
- 芯片：ESP32-C3 QFN32 rev v0.4，40MHz，MAC 9c:cc:01:60:02:d4（esptool 认）。

### 顺带确认：STA + 网页这条路是通的
- 重启日志里出现过 `配置页面 index.html（65607 字节）按 1024 字节分块发送`
  —— 有客户端真的把首页取走了（我的电脑连 5G、板子连 2.4G，疑似路由器
  做了频段/AP 隔离，所以我这边 ping/HTTP 都打不通，**用户侧能通**）。
- ping 不通是正常的：MicroPython 的 lwIP **不回 ICMP**，不能用 ping 判定
  板子在不在线。
- 我这边 `Invoke-WebRequest /` 报"连接被意外关闭"、`/index.html` 返回
  **404** —— 404 说明 HTTP 服务确实在响应。

### 产出
- 新增 `tools/serial_provision.ps1`：**串口配网**，绕开坏掉的 AP，直接把
  `SSID;密码` 写进 wifi.dat 让板子连路由器（密码不落盘、日志打码）。
  用法：`.\tools\serial_provision.ps1 -Port COM22 -SSID xxx -Password yyy`。
- 新增 `%TEMP%\pushall.ps1`：整片擦除后**全量回推** 19 个文件（含新建
  `bambu/` 目录），逐行 base64 写入 + 每个文件 SHA256 回读校验。本次
  一次通过（ALL_FILES_OK），板子已完全恢复。
- 注意：排查中我整片擦过 flash，并已用上面的脚本把应用全量推回；
  wifi.dat 里现在写着 xiaomi2（我通过串口写入的），板子会自动连它，
  IP 192.168.2.153。

### 给用户下一步（等他确认）
1. 手机贴到板子 10cm 内搜 AMS_WIFI（排除"功率极低"）。
2. 换 USB 口 / 短线 / 外接 5V 电源（boot.py 自检一直在提示欠压复位，
   供电嫌疑大）。
3. 看模块型号：若带 IPEX 座且没插外接天线 → 就是它。
4. 都不行 → 换一块 ESP32-C3 模块对比。

## 傍晚：「网页打不开」根因 = C3 内存不够（不是网络问题）+ 双开发板改造

用户强烈反馈：加了 OTA 功能之后网页彻底打不开（之前"只是慢但能用"），
要求「增加 ESP32S3 42 针、同时编译两块板的固件、IO 冲突就拆成两个配置文件、
编译自动调用对应配置、把 S3 剩余可用 IO 全部引出」。

### 1) 「网页打不开」的真正根因（两层，第二层才是致命的）

| 层 | 现象 | 结论 |
|---|---|---|
| 表层 | ESP32 默认 WiFi modem-sleep（`pm=1`），持续传输时周期性休眠 → STA 掉线，串口刷 `ECONNABORTED` | `sta.config(pm=0)` 关掉省电后，空应用下 65KB 页面 5.7 秒发完 |
| **根因** | **C3 只有 400KB SRAM**。不加载应用时空闲堆 149KB；**加载应用后只剩约 60KB**，最大连续块更小。WiFi 驱动收发时要动态申请**连续**缓冲 → 申请不到就 `OSError(113)`，重传 9.4 秒后放弃、射频卡死只能复位 | `probe_sf5`（app 已加载、阻塞式 accept、无 asyncio）仍 `ERR OSError(113) free=60256`；`probe_mem_app` 显示光 `ota_update` 就吃 15968B |

→ 加 OTA 之后应用变大，把「偶发」推成「必然」。**pm=0 是必要的，但在 C3 上
不充分**；真正的解法是换 S3。pm=0 与「发送后 drain 再关连接」两处修复已保留
（对 S3 也有益）。

### 2) 双开发板改造（提交 b361fba，本地提交，未 push）

架构：**同一套业务代码 + 两套引脚表 + 编译期写死板型**

- `python_code/board_c3.py` / `board_s3.py`：只描述"这块芯片哪些脚能用"
  （默认接线 / 保留脚 / strapping / 安全输出脚 / 剩余可用 IO）
- `python_code/hardware_config.py`：只剩「选板型 + 业务参数 + 上电自检」，
  导出所有引脚常量，上层代码一行不改
- 板型优先级：**编译期 `board_select.py`（BOARD="c3"/"s3"/"auto"）** >
  现场 `board_override.py`（可覆盖任意引脚，不进 git）> `os.uname().machine`
  识别（认不出按 C3 并提示）

S3 默认接线：电机 4/5、离合 6/7/8/9（**全干净脚**）、状态灯 2。
C3 上 4 号离合只能挤在 strapping 的 GPIO3（带病运行），S3 没这个问题。
S3 剩余可用 IO **全部列出**：18 只干净脚（含到位开关推荐的 11~14）
+ 4 只空闲 strapping 脚（0/3/45/46）。

构建：
- `python tools/build_mpy.py`（默认 `--board both`）→ `dist/c3/`、`dist/s3/`
  + `dist/esp32c3-ams-mpy.zip`、`dist/esp32s3-ams-mpy.zip`；
  编译时生成 `board_select.py`，**部署说明里直接写出该板实际接线与剩余可用脚**
- `python tools/make_firmware_bin.py --board c3|s3 --chip esp32c3|esp32s3`
  → 暂存源码目录（`dist/_work/src-<board>`）里写入 `board_select.py` 再打包，
  不污染版本库；产物默认 `dist/<芯片>-ams-firmware.bin`
- `python tools/make_update_pack.py --board keep|c3|s3|auto`，默认 keep
  （OTA 只换业务代码，不动设备原有板型设置）
- CI 改成 matrix：C3/S3 各自的 .mpy 包与单文件固件一起构建、一起发布

关键实测数据（已核对）：
- **官方固件分区表**：C3 `vfs @0x200000 size 2MB`（总 4MB）；
  S3 `vfs @0x200000 size 6MB`（**总 8MB → S3 单文件固件必须烧在 8MB 及以上模组**）
- S3 官方固件 `ESP32_GENERIC_S3-20240602-v1.23.0.bin`，
  SHA256 `b91080af2e9b78bad4308f98bb6187567cae24ed77cd7f48ef99b47af3ef0555`
  （C3 那份还是 `8058b7d6…`，没变）

测试：新增 8 项（两份引脚表都校验、板型优先级、S3 剩余 IO 数量）→ **124/124 全绿**。

### 环境备忘
- mpy-cross 1.23.0（mpy v6.3）已装在隔离环境
  `C:\Users\lkfcs\.workbuddy\binaries\python\envs\default\Scripts\python.exe`，
  用这个解释器跑构建/自测即可
- **本机没有 gcc**，编译不出 `tools/build/lfs_mkfs`，所以单文件固件只能在 CI 里出；
  本地已验证分区解析 + `stage_source` 板型下发逻辑（用真实官方固件跑的）
- 官方固件缓存在 `dist/_cache/`（C3 + S3 两份都在），离线构建可直接复用
- ⚠️ **Bash 工具在这台机器上基本不可用**（无 coreutils：`dirname`/`head`/`tail`/`cat`
  全没有）；`git` 和 `python` 能用。PowerShell 工具可靠，但**标准输出会被吞**，
  要拿结果就写文件再用 Read 读。cmd.exe 被安全策略禁用。

## 深夜：ESP-AMS-S3 —— 用 ESP-IDF 把整个项目重写成 C

用户要求：新建 `esp-ams-s3/`，**按给定目录结构用 ESP-IDF 重写**；每路电磁配
「停止送料 / 开始送料」两个微动，IO 够就再加「自吸微动」，自吸触发后自动送料、
**收到挤出机到位信号后蠕动送料 3 次**确保耗材到位。

### 结论：3.8MB 静态 SRAM 那类约束从架构上消失了（这是重写的最大收益）

Python 版 main.py 的阶段划分（先开射频 → 预建 `:80` socket → 最后才 import
应用）**全部**是为了绕开"GC 不压缩、最大连续块只剩 3584 字节"。ESP-IDF 下
WiFi 驱动用静态缓冲，`esp_http_server` / `esp-mqtt` 各自跑在独立任务里，
所以：**handler 可以放心写阻塞代码**（扫 WiFi 2 秒、OTA 几十秒都行），
启动顺序可以按逻辑依赖排而不用按内存排。另外有了真正的 **ota_0/ota_1 双分区
OTA**（Python 版单 factory 分区做不到整机固件升级）。

### 结构（22 个 .c/.h）

`main/{main.c, board_pins.h, config.c/h, wifi_manager.c/h, web_server.c/h,
bambu_mqtt.c/h, bambu_proto.c/h, motor.c/h, clutch.c/h, filament_sensor.c/h,
ams_controller.c/h, log_buffer.c/h}` + `web/{index.html,app.js,style.css}` +
`partitions.csv` + `sdkconfig.defaults` + `tools/lint_c.py` + `README.md`。
（比用户给的结构多 3 个文件：`board_pins.h`=引脚表、`bambu_proto.c/h`=报文解析、
`log_buffer.c/h`=日志环。`mqtt_client.c/h` **必须改名** `bambu_mqtt.c/h`——
IDF 自带 `mqtt_client.h`，同名会把官方头文件遮住导致找不到 `esp_mqtt_client_init`。）

引脚：电机 4/5，离合 6/7/8/9，LED 2，通道1~4 的停止/开始/自吸微动
= 11/12/13、14/15/16、17/18/21、38/47/48，挤出机到位 GPIO1。
剩余可用：10、39~42（干净）+ 0/3/45/46（strapping，仅输入）。

### 本轮修掉的三类真问题（都是"没有编译器就必须靠人眼"的类型）

1. **`esp_get_revision()` 这个 API 根本不存在** → 真名是 `esp_chip_info()`
   填 `esp_chip_info_t`。且 IDF v5.0 起 `esp_chip_info.h` 不再由 `esp_system.h`
   间接包含，必须显式 include。`chip.revision` 格式也变过（v5 是 `100*大+小`）。
2. **`bambu_proto.c` 里中文字符串嵌了裸 ASCII 双引号**
   （`"可能检测到"意面"缺陷…"`）→ 硬语法错误。★ 关键是**这行引号总数是偶数**
   （6 个），所以"数引号奇偶"的判据完全看不见 —— 见下面 lint 的改进。
3. **`OTA_WITH_SEQUENTIAL_WRITES` 是 v3.x 旧名**（v4.0 起改名 `OTA_SIZE_UNKNOWN`，
   值都是 `(size_t)-1`），新版头文件已删掉它 → 改成 `OTA_SIZE_UNKNOWN`。

另外自查出一个**硬件安全缺陷**（不是编译问题）：`motor_apply()` 原来是
"先写 IN1 再写 IN2"，从退料切进料的瞬间**两路同时为高 → H 桥直通 → 烧驱动芯片**，
而函数注释却声称已保证"两路不同时为大"。已改成**先双路清零、再给目标通道赋值**，
并拦截"两路都非 0"的调用。这条和离合的四重仲裁同级，属于硬件红线。

同时从 `sdkconfig.defaults` 删掉两个**不存在的 Kconfig 符号**（只会换来
"unknown config item" 警告并让人误以为已生效）：
- `CONFIG_ESP_WIFI_POWER_SAVE_NONE` —— 根本没这个符号。WiFi 省电在 ESP-IDF 里
  是**运行时**设置，唯一正确位置是 `esp_wifi_set_ps(WIFI_PS_NONE)`
  （已在 `wifi_mgr_init()` 里）。
- `CONFIG_SPIRAM_TYPE_AUTO` —— v4.x 的符号，v5.0 起该 choice 已删除。

### `tools/lint_c.py` 判据被真实 bug 逼着升级（★ 可复用）

原判据是"每行引号数量为奇数 → 可疑"，**被第 2 条 bug 打脸**（那行是偶数）。
改成 **"代码位置出现中文字符"**：C 的标识符只能是 ASCII，中文合法出现的位置
只有字符串和注释，所以剔掉字符串+注释后还能扫到中文 → 一定是字符串被提前截断、
后面的中文掉出来变成了"代码"。**这条零误报**，比奇偶可靠得多。

新增 `--selftest`（8 条内置用例：3 条"必须抓到" + 5 条"不该误报"，含
跨行括号配平、JSON 转义 `\"`、中文只在注释里）。改判据前先跑它。
结果为让 selftest 能喂内存样例，`check_file()` 拆成 `读取 → check_lines(lines)`。
两个命令都返回非 0，可直接当 CI 门禁。

### 验证状态（重要，别当成"已经能跑"）

- ✅ `lint_c.py` 扫 22 个文件 = **0 处可疑点**；`--selftest` **8/8 通过**
- ✅ 跨模块「声明 ↔ 定义 ↔ 调用」逐项比对无缺失；ESP-IDF v5 API 逐项核对
  （含最容易错的 `esp_mqtt_client_config_t` v4→v5 嵌套字段：
  `.broker.address.uri` / `.credentials.authentication.password` / `.session` /
  `.network` / `.buffer`，项目里写对了）
- ⏳ **`idf.py build` 还没跑过** —— 本机**没有 ESP-IDF 工具链**（也没有 gcc）。
  **这份代码还没被编译器编译过一次。**
- 新增 `.github/workflows/esp-ams-s3-build.yml`（**只在这个目录改动时触发**，
  与原有 MicroPython 流水线完全隔离，不发布产物）：
  先 `lint_c.py --selftest` + 扫源码，再用官方 `espressif/esp-idf-ci-action@v1`
  （Docker 镜像 `espressif/idf:v5.3.2`，target=esp32s3，path=esp-ams-s3）跑
  `idf.py build`，最后检查应用体积是否 ≤ `ota_0` 分区上限 2031616 字节。
  → **推分支即验证**，这是目前唯一能真正回答"编不编得过"的办法。

### 项目约定（新增）

- **必须 ESP-IDF v5.x，不能降到 v4.x**：v4 的 `esp_mqtt_client_config_t` 还是
  扁平字段（`uri`/`username`/`password`），会直接编译失败。
- `main/CMakeLists.txt` 的 `REQUIRES` 里 **必须留 `esp_psram`**（哪怕一行它的
  API 都没调）：`CONFIG_SPIRAM*` 这些选项定义在 `esp_psram` 组件自己的 Kconfig
  里，组件不在依赖列表里 Kconfig 就可能不被解析 → 配置被当"未知项"丢掉、
  PSRAM 悄悄没打开（不报错，只是内存少了）。原来占位的 `spiffs` 已移除（未使用）。
- 网页三个文件用 `EMBED_FILES` **编进固件 `.rodata`**，不挂文件系统 ——
  没有"SPIFFS 挂载失败 → 网页打不开"这类故障，也没有额外工具依赖。
- `main.c` 最后一步 `if (web_ok) esp_ota_mark_app_valid_cancel_rollback();`
  **不能省**：开了 `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` 却不确认，下次重启
  会自动回滚，"升级成功但一重启变回旧版"，极难查。放在最后 = 只有体检通过才确认。
- 4MB Flash 模组装不下默认分区表（5MB），README 里给了替代 `partitions.csv`
  （两块 app 各 1.75MB、去掉 storage，总 3.63MB）。

### 环境备忘（补一条更省事的取输出姿势）

PowerShell 标准输出会被吞 → **直接用 python 自己写 UTF-8 文件再 Read**，
比 `Out-File` 干净（`Out-File -Encoding utf8` 在 `PYTHONIOENCODING` 没设时会把
UTF-8 字节按 GBK 解码再编码，出来是双重乱码）：

```bash
"C:/Users/lkfcs/.workbuddy/binaries/python/versions/3.13.12/python.exe" -c "
import sys, runpy
sys.stdout = open(r'C:\...\out.txt','w',encoding='utf-8')
sys.argv=['lint_c.py','esp-ams-s3']
runpy.run_path('esp-ams-s3/tools/lint_c.py', run_name='__main__')
"
```

另外：`Remove-Item` 多路径列表那次**静默失败**了（文件还在），改逐个删 + 用
Glob/Python `os.remove` 复核才确认删掉。删文件后**一定要用 Glob 或 Python 复核**。

